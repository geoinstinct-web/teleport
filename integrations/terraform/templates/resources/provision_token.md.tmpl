---
title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

This is a custom template overriding the default one.

This allows us to provide more examples, custom text while keeping the schema generation.


## Example Usage

This is a regular provision token.

{{tffile "./examples/resources/teleport_provision_token/resource.tf" }}

This is an IAM one:

{{tffile "./examples/resources/teleport_provision_token/iam.tf" }}

{{ .SchemaMarkdown | trimspace }}
{{- if .HasImport }}

## Import

Import is supported using the following syntax:

{{codefile "shell" .ImportFile }}
{{- end }}

